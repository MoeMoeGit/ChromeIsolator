# 当前状态

> 更新时间：2026-09-26
> 核对依据：1.7.10 源码与双仓库自查结果
> 阶段：源码版本 1.7.10，最新已发布版本仍为 1.7.9

## 活跃任务

双仓库检查与修复纳入 1.7.10 源码同步，本次包含全部工作区代码、测试和文档；安装包尚未发布。用户已确认：新建环境默认开启采集；有效配置对应目录缺失时保留信息并补建目录。

## 当前能力与变更

- 保留已有环境管理、托盘、Chrome/Edge 引导、外链、七语言、四种模式、ZIP/MSI 能力。
- 新建默认值与 macOS 一致，旧配置及磁盘重建不迁移为采集开启；首次生成配置立即保存。
- 修复关闭期间的进程跟踪、重复关闭、旧退出回调、端口预留冲突、外链关闭竞态、异步磁盘扫描集合变化。
- 配置空值进入恢复链路，损坏副本尽力保留，保存失败保留内存状态并提示；缺失目录保留元数据。删除先保存配置，移动失败回滚，避免保存失败后补建空环境。
- 批量采集按筛选结果一次保存，无可修改项时禁用，空筛选显示提示；高级引擎信息刷新与七语言默认环境名已修复。
- CDP 清理不再无限等待关闭握手，连接超时进入重试。

## 验证与限制

- 临时 .NET 8.0.100 在 macOS 交叉编译 WPF，通过且 0 警告；未升级项目目标框架或依赖。
- `tests/CompatibilityTests` 使用生产配置/端口源码及临时假数据验证，已接入 Windows CI；30 项断言通过（含删除回滚与受控子进程启停）；七语言资源键一致，文档与差异格式检查通过。
- 未运行 Windows UI、真实 Chrome/Edge/CDP、回收站、默认应用注册、管道外链、MSI 安装升级。交叉编译不能替代这些验收。
- 综合对齐报告保存在 macOS 仓库的 `docs/windows-alignment.md`。代码自查不是独立评审。

## 待讨论与回归

- Windows 浅色主题与 macOS 深浅色支持不同，是否补齐深色模式尚未决定。
- Windows 正常配置加载时会补入额外磁盘环境；macOS 仅在恢复时扫描，两端是否统一自动发现需决定。
- 真实进程立即退出、关闭超时、连续启停、多环境端口冲突和差异模式重连需 Windows 10/11 回归。
- 官方下载页面 `.com` 优先及 `.cn` 回退、浏览器签名校验仍待规划。
